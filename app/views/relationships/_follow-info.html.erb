<div class="follow-wrapper">
    <div class="follow-count">
        <%= link_to user_followings_path(user) do %>
            <p class="follow-title">フォロー</p><span class="follow-number"><%= user.followings.count %></span>
        <% end %>
    </div>
    <div class="follow-count">
        <%= link_to user_followings_path(user) do %>
            <p class="follow-title">フォロワー</p><span class="follow-number"><%= user.followers.count %></span>
        <% end %>
    </div>
</div>
